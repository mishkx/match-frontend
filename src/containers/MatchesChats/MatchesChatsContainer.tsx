import React from 'react';
import { ChatsCollectionContainer, MatchesCollectionContainer } from '../index';

const MatchesChatsContainer: React.FC = () => (
    <>
        <MatchesCollectionContainer/>
        <ChatsCollectionContainer/>
    </>
);

export default MatchesChatsContainer;
